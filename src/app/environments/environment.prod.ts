export const environment = {
  production: true,
  // Placeholder — real production domain will replace this once deployed.
  // NEVER a secret/API key here: this file ships to every browser that loads
  // the app, fully readable by anyone via browser dev tools.
  apiUrl: 'https://api.saman.example.com/api'
};